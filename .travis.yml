language: csharp
mono:
 - beta

install: curl -sSL https://raw.githubusercontent.com/aspnet/Home/dev/dnvminstall.sh | sh && source ~/.dnx/dnvm/dnvm.sh && dnvm upgrade

script:
  cd codevalr-api/src/codevalr && dnu restore && dnu build && cd ../codevalr-api && dnu restore && dnu build

# whitelist
branches:
  only:
    - master
